# ANG HR BETA-v0.1 朋友測試說明

## 這一版可以測什麼

這是 **手機優先的前端 Beta／流程 Demo**。可以測試頁面、導航、排班、請假、審核、人員、公告、設定、手動打卡與薪資示範流程；不是正式上線版。

> 請只輸入虛構測試資料。不要輸入真實身分證、薪資、醫療文件、公司機密或任何個資。

## 測試網址

- 固定 Beta：`http://192.168.0.242:8125/`
- 僅限與測試電腦位於同一 Wi-Fi／區域網路。
- 畫面必須顯示 `BETA-v0.1`；若不是，請回報並附截圖。

## 臨時測試帳號

以下全部都是固定假資料，只能用於 `BETA-v0.1`。本版入口只接受這 6 組臨時資料。管理員與員工共用臨時密碼 `AngBeta#2026`，入口會核對「帳號＋Email＋臨時密碼」；免費用戶使用固定驗證碼 `123456`。

### 企業管理員（2 組）

| 組別 | 入口 | 管理帳號 | Email | 臨時密碼 |
|---|---|---|---|---|
| 管理員 A | 企業用戶 | `ANG-BETA-ADM01` | `admin01@ang-beta.test` | `AngBeta#2026` |
| 管理員 B | 企業用戶 | `ANG-BETA-ADM02` | `admin02@ang-beta.test` | `AngBeta#2026` |

### 員工／個人用戶（2 組）

| 組別 | 入口 | 員工帳號 | Email | 臨時密碼 |
|---|---|---|---|---|
| 員工 A | 個人用戶 | `ANG0601` | `employee01@ang-beta.test` | `AngBeta#2026` |
| 員工 B | 個人用戶 | `ANG0602` | `employee02@ang-beta.test` | `AngBeta#2026` |

### 免費新用戶（2 組）

| 組別 | 入口 | 測試識別碼 | Email | 固定驗證碼 |
|---|---|---|---|---|
| 免費 A | 免費用戶 | `FREE-BETA-01` | `free01@ang-beta.test` | `123456` |
| 免費 B | 免費用戶 | `FREE-BETA-02` | `free02@ang-beta.test` | `123456` |

- 免費流程不會真的寄信，畫面取得測試碼後直接輸入 `123456`。
- Google 與 LINE 登入尚未串接，Beta 內已停用。
- 目前入口不是正式帳密系統，沒有帳號鎖定、停權、方案到期、登入嘗試限制或 Token 驗證；它只負責把測試者帶到指定角色工作區。
- 前端工作區依角色分流：企業管理員進管理端；員工、個人與免費用戶只進員工端。切換帳號時一律返回入口重新登入；這不等同正式伺服器權限隔離。
- 兩組同角色帳號目前共用該角色的本機 Demo 工作區，不代表已完成獨立帳戶與跨裝置同步。


## 建議測試流程

1. 分別使用兩組管理員與兩組員工臨時帳號，確認會進入正確角色工作區。
2. 以員工帳號開啟身分選單，確認只能返回入口切換臨時帳號，不能直接進管理端。
3. 使用管理員帳號登入，新增一位虛構員工並查看人員列表。
4. 建立排班，只按「儲存草稿」；員工端不應看到草稿。
5. 管理員發布班表，再切換員工端查看本週／下週班表。
6. 返回入口，以員工帳號登入。
7. 送出一筆請假申請。
8. 返回入口，以管理員登入，在審核中心核准或退回。
9. 再回員工端，確認請假狀態同步。
10. 測試「手動測試打卡」，確認文字明確標示未驗證 NFC／定位。
11. 重新整理頁面，確認同一分頁仍保持目前測試身分。
12. 登出後確認回到入口。
13. 測試日光／暗夜模式、上下滑動、卡片展開收合與左右滑動。

## 資料限制

- 所有資料只保存在目前瀏覽器，不會同步到朋友的其他手機。
- 測試帳號、Email、密碼與驗證碼皆為公開的固定假資料；不可拿來保護任何真實資料。
- 不可輸入真實姓名、電話、地址、身分證號、銀行帳號、薪資、醫療資訊、公司機密或正式登入密碼。
- 清除瀏覽器網站資料會刪除全部測試紀錄。
- 檔案上傳只保存檔名與分類，不會傳送實體檔案。
- 薪資、工時、定位與部分公告資料含示範值。

## 回報格式

請提供：

1. 版本：`BETA-v0.1`
2. 裝置與瀏覽器
3. 使用身分：管理員／員工／免費新用戶
4. 操作步驟
5. 預期結果
6. 實際結果
7. 截圖或錄影
8. 是否每次都會發生

收到回報後會加入 `BETA_ISSUE_TRACKER.md`，保留發現來源並依 P0～P3 分級。
